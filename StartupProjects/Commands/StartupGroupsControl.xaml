<UserControl x:Class="StartupProjects.Commands.StartupGroupsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <ToolBarPanel DockPanel.Dock="Top">
                <ToolBar ToolBarTray.IsLocked="True" Loaded="ToolBar_Loaded">
                    <Menu ToolBar.OverflowMode="Never">
                        <MenuItem Name="mnuNew" IsEnabled="False" Header="New..." Click="MenuItem_Click" />
                        <MenuItem Name="mnuEdit" IsEnabled="False" Header="Edit..." />
                        <MenuItem Name="mnuDelete" IsEnabled="False" Header="Delete..." />
                    </Menu>
                </ToolBar>
            </ToolBarPanel>
        </DockPanel>

        <TreeView x:Name="trvGroups" Grid.Row="1" />

    </Grid>
</UserControl>

